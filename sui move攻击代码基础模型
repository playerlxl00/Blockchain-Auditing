在 Sui Move 的测试体系中，单元测试、覆盖率测试和模糊测试构成了三道层层递进的防线。它们各自承担不同的使命：单元测试验证逻辑正确性，覆盖率测试确保测试完整性，模糊测试则发掘前两者难以触及的边界漏洞。本文逐一详解这三类测试在 Sui Move 语境下的概念与实践。

1. 单元测试：覆盖边界条件
1.1 概念解释
单元测试是对软件中最小可测试单元进行独立验证的测试方法，在 Sui Move 中通常是单个函数或模块。边界条件指函数输入或系统状态的极端情况——例如零值、最大值、空值、溢出边界等。覆盖边界条件的意义在于：绝大多数运行时错误和资产损失漏洞都发生在这些边缘路径上，而非正常路径。

在 Sui Move 中，单元测试使用以下核心注解：

· #[test]：将无参函数标记为测试函数，执行 sui move test 时框架自动发现并运行

· #[test_only]：标记仅测试模式下编译的代码，不会打包进生产字节码

· #[expected_failure]：标记预期失败的测试，可指定 abort code 精确定位错误类型

此外，Sui 提供 test_scenario 模块，可以模拟多地址、多交易的复杂链上场景。

1.2 代码示例与逐段详解
假设我们要实现一个简易的资产模块，核心业务是铸造和燃烧 Coin，并设定铸造数量必须大于零、燃烧数量不能超过持有量的业务规则。

业务模块：sources/coin.move
move
module test_demo::coin {
    use sui::object::{Self, UID};
    use sui::tx_context::TxContext;
    use sui::transfer;
    use sui::balance::{Self, Balance};

    /// 错误码：铸造金额必须大于0
    const E_INVALID_MINT_AMOUNT: u64 = 1;
    /// 错误码：余额不足
    const E_INSUFFICIENT_BALANCE: u64 = 2;

    /// Coin 对象，拥有 key 能力，可作为链上独立对象
    public struct Coin has key {
        id: UID,
        balance: Balance<COIN>,
    }

    /// 铸造新 Coin 并转移给指定地址
    public fun mint(amount: u64, recipient: address, ctx: &mut TxContext) {
        // 边界条件：amount 必须大于 0
        assert!(amount > 0, E_INVALID_MINT_AMOUNT);
        let balance = balance::create_for_testing(amount);
        let coin = Coin { id: object::new(ctx), balance };
        transfer::public_transfer(coin, recipient);
    }

    /// 燃烧指定数量的 Coin
    public fun burn(coin: &mut Coin, amount: u64) {
        let current = balance::value(&coin.balance);
        // 边界条件：燃烧金额不能超过当前余额
        assert!(amount <= current, E_INSUFFICIENT_BALANCE);
        balance::burn(&mut coin.balance, amount);
    }

    /// 获取 Coin 余额（辅助函数）
    public fun balance(coin: &Coin): u64 {
        balance::value(&coin.balance)
    }
}
测试模块：sources/coin_tests.move
以下测试覆盖了四个核心边界条件：正常铸造、零值铸造、全额燃烧、超额燃烧。

move
#[test_only]
module test_demo::coin_tests {
    use test_demo::coin::{Self, Coin};
    use sui::test_scenario::{Self, Scenario};
    use sui::tx_context;

    // 错误码常量，与业务模块保持一致
    const E_INVALID_MINT_AMOUNT: u64 = 1;
    const E_INSUFFICIENT_BALANCE: u64 = 2;

    // ============ 边界测试 1：正常铸造 ============
    #[test]
    fun test_mint_success() {
        let owner = @0xA;
        let mut scenario = test_scenario::begin(owner);

        // 铸造 100 个 Coin，属于边界内的正常值
        coin::mint(100, owner, test_scenario::ctx(&mut scenario));
        // 推进到下一个交易，使对象进入全局池
        test_scenario::next_tx(&mut scenario, owner);

        // 从发送者地址取出 Coin 对象
        let coin = test_scenario::take_from_sender<Coin>(&scenario);
        // 断言余额正确
        assert!(coin::balance(&coin) == 100, 0);
        // 归还对象，避免因无 drop 能力导致编译错误
        test_scenario::return_to_sender(&scenario, coin);

        test_scenario::end(scenario);
    }

    // ============ 边界测试 2：零值铸造（预期失败）============
    #[test]
    #[expected_failure(abort_code = E_INVALID_MINT_AMOUNT)]
    fun test_mint_zero_fails() {
        let owner = @0xB;
        let mut scenario = test_scenario::begin(owner);

        // amount = 0，触达下边界，预期 abort 且错误码为 E_INVALID_MINT_AMOUNT
        coin::mint(0, owner, test_scenario::ctx(&mut scenario));

        test_scenario::end(scenario);
    }

    // ============ 边界测试 3：正常燃烧 ============
    #[test]
    fun test_burn_success() {
        let owner = @0xC;
        let mut scenario = test_scenario::begin(owner);

        // 先铸造 100
        coin::mint(100, owner, test_scenario::ctx(&mut scenario));
        test_scenario::next_tx(&mut scenario, owner);

        let mut coin = test_scenario::take_from_sender<Coin>(&scenario);
        // 燃烧 40，属于正常范围内的部分燃烧
        coin::burn(&mut coin, 40);
        assert!(coin::balance(&coin) == 60, 0);
        test_scenario::return_to_sender(&scenario, coin);

        test_scenario::end(scenario);
    }

    // ============ 边界测试 4：超额燃烧（预期失败）============
    #[test]
    #[expected_failure(abort_code = E_INSUFFICIENT_BALANCE)]
    fun test_burn_exceeds_balance_fails() {
        let owner = @0xD;
        let mut scenario = test_scenario::begin(owner);

        // 先铸造 50
        coin::mint(50, owner, test_scenario::ctx(&mut scenario));
        test_scenario::next_tx(&mut scenario, owner);

        let mut coin = test_scenario::take_from_sender<Coin>(&scenario);
        // 燃烧 51，超过持有量，触达上边界，预期失败
        coin::burn(&mut coin, 51);
        test_scenario::return_to_sender(&scenario, coin);

        test_scenario::end(scenario);
    }

    // ============ 辅助测试：测试模式下的上下文创建 ============
    #[test_only]
    public fun dummy_context(): sui::tx_context::TxContext {
        sui::tx_context::dummy()
    }
}

代码详解
（1）#[test_only] 模块声明
测试模块使用 #[test_only] 标注，确保此模块仅在 sui move test 模式下编译，不会污染生产构建。

（2）test_scenario 多交易模拟

· test_scenario::begin(address) 初始化一个模拟场景，返回 Scenario 对象。该对象维护一个全局对象池，所有测试中的对象都存储于此。

· test_scenario::next_tx(&mut scenario, sender) 将场景推进到下一个交易。这是必须的——在铸造 Coin 后，Coin 对象还停留在交易的“临时状态”中，只有调用 next_tx 后才会进入全局池，之后才能被 take_from_sender 取出。

· test_scenario::end(scenario) 在测试末尾显式清理 Scenario 对象。由于 Scenario 不具备 drop 能力，不清理会导致编译错误。

（3）#[expected_failure(abort_code = ...)] 的精确用法
在 test_mint_zero_fails 和 test_burn_exceeds_balance_fails 中，我们使用了带 abort_code 的 expected_failure。这不仅是告诉框架“这个测试会失败”，更是精确指定失败原因。如果因其他原因 abort（比如类型不匹配、gas 不足），测试会判定为失败。这避免了“假阳性”——测试以为捕获到了想要的错误，实际却是另一个 bug 导致的失败。

（4）take_from_sender 与 return_to_sender

· take_from_sender<T> 从当前交易的发送者地址取出类型为 T 的对象。

· return_to_sender 将对象归还。这是因为测试中取出对象后，该对象脱离了全局池的管理，如果不归还，test_scenario::end 时对象计数会出错，导致 TransactionEffects 构建失败。

（5）边界条件的覆盖策略
四个测试分别覆盖了：

· 正常值（100, 40）→ 验证函数在正常条件下正确执行

· 下边界零值（0）→ 验证防御性断言能正确拒绝无效输入

· 上边界超额（51 > 50）→ 验证余额保护逻辑的正确性

运行测试命令：

bash
sui move test
预期输出四个测试全部 PASS。

2. 覆盖率测试：消灭盲点
2.1 概念解释
代码覆盖率是衡量测试执行了多少代码的量化指标，通常以百分比表示。覆盖率测试的目的是消灭盲点——找出那些从未被任何测试执行到的代码路径（即“死代码”或“漏测代码”）。在智能合约开发中，未被覆盖的代码等同于未经检验的潜在漏洞。

Sui Move CLI 提供了完整的覆盖率工具链：

命令	                                          作用
sui move test --coverage	                      运行测试并收集覆盖率数据
sui move coverage summary	                      显示各模块覆盖率汇总
sui move coverage source --module <name>	      逐行显示源码覆盖情况
sui move coverage lcov	                        生成 LCOV 格式报告，供 CI/CD 工具使用
2.2 实践流程与示例
bash
# 第一步：运行测试并收集覆盖率数据
sui move test --coverage

# 第二步：查看模块级别的覆盖率汇总
sui move coverage summary

# 示例输出：
# +-------------------------+
# | Move Coverage Summary   |
# +-------------------------+
# Module 0x0::coin >>> % Module coverage: 100.00
# Module 0x0::coin_tests >>> % Module coverage: N/A (test_only)
# +-------------------------+
# | % Move Coverage: 100.00 |
# +-------------------------+
若覆盖率未达 100%，可使用以下命令查看具体哪些行未被覆盖：

bash
# 查看指定模块的源码行覆盖情况
sui move coverage source --module coin
输出会在源代码中标注每一行是否被执行过，未被覆盖的行会被明确标出。这对于定位测试盲区至关重要。

2.3 持续集成中的覆盖率报告
在 CI/CD 流水线（如 GitHub Actions）中，可生成 LCOV 格式报告并上传到 Codecov 等平台：

bash
# 生成 LCOV 报告
sui move test --coverage
sui move coverage lcov

# 此时当前目录会生成 lcov.info 文件
# 可配合 genhtml 生成 HTML 可视化报告
genhtml lcov.info -o coverage_html
这样每次提交 PR 时，团队都能看到覆盖率变化趋势，确保覆盖率不会随着代码迭代而下降。

3. 模糊测试：发现隐藏漏洞
3.1 概念解释
模糊测试（Fuzzing）是一种自动化的软件测试技术，通过向程序输入大量随机、非预期或畸形数据，观察程序是否出现崩溃、断言失败或其他异常行为。与单元测试“按剧本执行”不同，模糊测试“即兴发挥”，能发掘开发者从未设想过的问题。

在 Sui Move 语境下，模糊测试的价值尤为突出：

· 类型系统的约束使传统随机生成难以产生有效输入——Move 的强类型系统要求所有交易数据严格符合类型定义，盲目生成的数据 99% 会在入口处被拒绝，无法深入测试核心逻辑。

· 复杂的状态转换和多交易序列需要语义上连贯的输入序列，单纯随机无法模拟真实攻击场景。

目前 Sui Move 模糊测试生态中主要有两类工具：

工具	               特点
sui-fuzzer	         开源模糊测试器，支持无状态（单个函数）和有状态（函数调用序列）两种模式，使用 Rust 编写，通过配置文件驱动
Belobog	             MoveBit 团队推出的类型感知模糊测试框架，使用符号执行生成类型良好且语义可达的交易序列，在实验中对 Move 合约达到了 100% 的严重漏洞检出率
Movy	               专注于不变量测试的工具，通过 pre/post hooks 在模糊测试过程中验证合约不变量是否始终成立

3.2 sui-fuzzer 使用示例
配置文件 config.json
json
{
    "use_ui": true,
    "nb_threads": 8,
    "seed": 4242,
    "contract": "./build/coin/bytecode_modules/coin.mv",
    "execs_before_cov_update": 10000,
    "corpus_dir": "./corpus",
    "crashes_dir": "./crashes",
    "fuzz_functions_prefix": "fuzz_",
    "max_call_sequence_size": 5
}
字段说明：

· use_ui：是否启用终端 UI 实时显示 fuzzing 进度

· nb_threads：并行线程数，用于加速测试

· seed：随机种子，固定后可复现 crash

· contract：编译后的 .mv 字节码文件路径

· corpus_dir / crashes_dir：保存有趣的输入样例和导致崩溃的输入

· max_call_sequence_size：状态模式下的最大调用序列长度

运行命令
bash
# 无状态模式（测试单个函数）
make CONFIG_PATH="./config.json" \
     TARGET_MODULE="coin" \
     TARGET_FUNCTION="fuzz_mint"

# 有状态模式（测试函数调用序列）
make CONFIG_PATH="./config.json" \
     TARGET_MODULE="coin" \
     TARGET_FUNCTIONS="mint,burn,transfer"
3.3 模糊测试的思路：定义不变量
模糊测试的核心不是“跑起来就行”，而是定义不变量（invariant）。不变量是程序在任何状态下都必须成立的属性，例如：

· 余额守恒：mint 增加的代币总量 + 销毁的代币总量 = 地址余额变化之和

· 权限隔离：非管理员永远无法调用 mint

· 数值安全：任何操作后，余额不会变为负数

借助 Movy 这类工具，开发者可以在模糊测试中插入 pre/post hooks，每次函数执行前后自动验证不变量。

3.4 模糊测试与单元测试的协同
模糊测试不是单元测试的替代品，而是补充品：

· 单元测试解决“已知的已知”——开发者明确设计的边界条件和业务场景

· 覆盖率测试解决“已知的未知”——开发者知道自己漏了哪部分测试

· 模糊测试解决“未知的未知”——开发者根本没想到会出问题的输入组合

三者构成完整的测试金字塔：单元测试是地基，覆盖率是度量仪，模糊测试是最后一道防线，负责捕捉那些“理论上不可能发生”的漏洞。

总结
测试类型	       核心目的	                        Sui Move 工具链	                                              典型应用场景
单元测试	       覆盖边界条件，验证单函数逻辑	      #[test] + test_scenario	                                      铸造/燃烧/转账的零值、最大值、余额边界
覆盖率测试	     消灭盲点，确保每行代码被测试	      sui move test --coverage + coverage summary/source/lcov	      代码审查、CI 质量门禁
模糊测试	       发现隐藏漏洞，检验不变量	          sui-fuzzer / Belobog / Movy	                                  复杂状态机、资产安全关键模块

三种测试方法协同使用，才能在 Sui Move 智能合约上链前构建足够坚固的安全防线。正如单元测试关注“该做的做对了”，覆盖率测试关注“该测的都测了”，而模糊测试则关注“想不到的也测了”。

详细阐释： https://chat.deepseek.com/share/qeg31emq6t7txgg6md
