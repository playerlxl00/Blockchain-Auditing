# 对于所有sui项目来说，其所有项目源代码都写在sources文件夹里，其中每一个.move文件就是一个module，整个项目就是一个上传到公网的package。整个项目当中只有soureces文件夹和Move.toml文件是需要我们关注的文件，顶多后期关注一下前端链接，但整个项目和核心就是这两个文件，其它文件比如build只是一些编译信息，根本都不是源代码，完全不需要考虑
// 1. 引入别的模块
// marketplace.move
use my_package::token;      // 引入另一个模块
use my_package::admin;      // 引入另一个模块

public fun buy() {
    token::transfer(...);    // 调用别的模块的函数
    admin::check_permission(...);
}
// 2. 使用别的模块定义的类型
// pool.move
use my_package::token::TOKEN;

struct Pool has key {
    coin: Coin<TOKEN>,      // 使用 token 模块定义的类型
    ...
}
3. 别的模块来调用你
// token.move
public fun mint(...) { ... }  // 公开的函数，等着被别的模块调用
就这三种。没有别的。

// 所以首先我们根本不需要理解非常复杂的项目结构，也不需要阅读巨量代码，因为就算是有多个.move项目文件，它内部操作是非常单一的，你就算是创建了500个object呢，操作也都是一样的，包括官网上sui move教程里也是非常多的分类讨论，而不是扩展内部的代码复杂程度。所以代码本身并不吓人，审计的核心是理解，找茬，和测试

// 注意： 
// 1. 不是所有东西都能被引用
// 只有标记为 public 的函数、结构体、常量才能被外部模块访问：
module my_package::token {
    public struct Coin { ... }           // ✅ 可以被其他模块使用
    struct InternalState { ... }         // ❌ 只能当前模块用
    
    public fun mint() { ... }            // ✅ 可以被其他模块调用
    fun helper() { ... }                 // ❌ 只能当前模块内部用
    
    public const VERSION: u64 = 1;       // ✅ 可以被其他模块读取
    const INTERNAL_LIMIT: u64 = 100;     // ❌ 只能当前模块用
}

// 2. 结构体的字段不能直接跨模块访问
即使结构体是 public，它的字段也只能在定义它的模块内部直接访问：

// token.move
module my_package::token {
    public struct Coin has key {
        value: u64,        // 字段
    }
    
    public fun get_value(coin: &Coin): u64 {
        coin.value  // ✅ 在自己的模块里可以访问字段
    }
}

// marketplace.move  
module my_package::marketplace {
    use my_package::token;
    
    public fun check(coin: &token::Coin) {
        // coin.value  ❌ 编译错误！不能直接访问
        let v = token::get_value(coin);  // ✅ 必须通过公开函数
    }
}
// 这是 Move 的封装性设计——防止外部代码直接操作内部状态。

------------------------------------------------------------------------------

<>(){}理解： https://chat.deepseek.com/share/xcblcrxtnj2hwqggt6

-------------------------------------------------------------------------------

在 Move 语言（Sui Move）里，Result 是一个内置的枚举类型，它有两种状态：

· Ok(value) —— 表示操作成功，并携带返回值

· Err(error_code) —— 表示操作失败，并携带错误码

.is_err() 是 Result 类型上的一个方法，作用是：
如果当前结果是 Err 变体，就返回 true；否则返回 false。

在你这段测试里：

move
assert!(transfer::try_transfer(coin, @0x4).is_err(), ENotAuthorized);

· transfer::try_transfer(coin, @0x4) 返回一个 Result 类型（一般是 Result<(), u64> 或类似）。

· 紧接着 .is_err() 检查这个结果是不是一个错误。如果转账因为权限不足失败了（返回 Err），.is_err() 就是 true，assert! 通过。如果意外成功了（返回 Ok），.is_err() 就是 false，assert! 会触发，测试终止并抛出 ENotAuthorized 错误码。

简单说：这行测试就是在断言 “这次转账必须失败”，.is_err() 就是用来判断“是不是真的失败了”。

----------------------------------------------------------------------------------------

PTB机制详细阐释： https://chat.deepseek.com/share/3frbfzjjnji26acwge

-----------------------------------------------------------------------------------------

在 Move 中，如果函数参数以 _ 开头（比如 _witness），意思是：“这个变量我接收了，但函数体内不会用它”。

如果不加 _ 前缀而直接写成 witness: WITNESS，但函数体里又没用到这个变量，编译器会报警告（未使用变量）。加上 _ 告诉编译器：我是故意不用的，别烦我。

在 Witness 模式中，这正是精髓：我们只关心传入的类型本身来证明权限，而不需要读取这个值里的任何数据（反正它经常是空结构体）。所以用 _witness 是一种惯用写法。

注意：_只能限制类型不能限制值，所以不能通过_具体某个值或者某个地址之类来取代assert!： https://chat.deepseek.com/share/iz8uj95f43g0t710kz

-----------------------------------------------------------------------------------------

sui move函数调用与private, public, public(friend)机制详细阐释： https://chat.deepseek.com/share/d3911wzj4jsmu5d1hk

------------------------------------------------------------------------------------------

drop含义阐释： https://chat.deepseek.com/share/9niou7nxpn5vlrh56j

key, store, copy含义阐释： https://chat.deepseek.com/share/3jdzy9hxw4bjhsexpc

------------------------------------------------------------------------------------------

自动丢弃功能(隐式)：

简单讲，自动丢弃就是：你把一个带 drop 能力的值（比如数字、布尔值）随手扔在那里不管它，编译器帮你打扫干净。它是被默默地、省心地销毁。

哪些东西可以“自动丢弃”？（有 drop 能力的常见类型）
· 基础类型：u8, u16, u32, u64, u128, u256, bool, address。

· 部分标准库结构体：String (Move自带的), vector (如果里面的元素也有 drop), Option (如果里面的值有 drop)。

· Sui 框架里的：Balance<T>, Supply<T>, Url 等。

· 你自己定义的：只要你在结构体定义时加了 has drop。

哪些东西不能自动丢弃？（没有 drop 能力的常见类型）
· 所有带 key 能力的对象：比如 Coin<T>, UID, TreasuryCap<T>, 各种 NFT。它们是“资产”，禁止随手乱扔。

· 你自己定义的 Hot Potato：比如 FlashLoanReceipt，强制你必须显式解构。

所以，“自动丢弃”就是编译器替你做了清理工作，而不像 Hot Potato 那样，你一忘了它编译器就立刻报错。

--------------------------------------------------------------------------------------------

witness, receipt和hot potato介绍与区分： https://chat.deepseek.com/share/j76p19kkd6b8n80wlc


let PurchaseReceipt { id, item, price, buyer } = receipt; 是什么？
这是模式匹配解构。把 receipt 这个结构体拆开，将其字段分别绑定到同名变量 id, item, price, buyer 上，同时 receipt 本身就被销毁了。

之后你就可以单独使用这些变量了。因为结构体本身已被解构消失，所以这也是“销毁没有 drop 能力的结构体”的标准方法。

---------------------------------------------------------------------------------------------

模运算(同余)介绍：https://chat.deepseek.com/share/udpnwzodug2rr680jd
核心： 周期，拆分
