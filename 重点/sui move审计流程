1 做好审计框架以及工具的总结
2 阅读项目方的要求，如流量控制，隐私政策等，明确审计工具和操作空间的范围
3 大致了解项目是做什么的，对应到审计框架中设置重点审计方向 (比如流动性池子的审计就重点在数学逻辑正确性)
4 大致浏览README，先仔细了解项目是做什么的
5 梳理README结构，明确README都提供了什么样的信息，scope它如何能帮助理解项目文件，matching，并在之后整个阅读module过程中参照README
6 数有多少个module，对整个module情况有清晰的框架了解
7 点进每一个module文件，梳理module文件的框架，通常讲是分层明确的
  (1) 导入库 (use sui::)
  (2) 引用其它文件中 (use 项目::)
  (3) 常数设置 (const )  // 数有多少个，列清楚，并结合README注释清楚每个都是干嘛用的
  (4) 结构体罗列 (struct )  // 数有多少个，把内容去掉单名字列清楚，并结合README注释清楚每个都是干嘛用的  // 重点标注出有copy和drop的结构体
  (5) 函数罗列 (fun )  // 数有多少个，把内容去掉单名字列清楚，并结合README注释清楚每个都是干嘛用的，并且结合README梳理清楚调用逻辑，顺序等
  (6) 项目方自己的test代码  // 先不管，我认为还是先以自己的测试为重，哪有自己测试自己的
8 对每个module文件都进行step7的操作之后，开始关注结构体和函数的具体内容，对比项目方宣称的目的，看项目方构想与代码是否能对应上
9 在step8的过程中，每一部分的alignment确认无误之后都用审计框架查一遍 (包括单点和整个流程)
10 上述是自己读的部分，结束之后在框架可使用工具范围内逐一核查，并逐一进行框架内测试
11 根据项目方要求，结合框架总结的common审计报告格式，出具审计报告
12 上述所有过程中但凡是遇到不懂的内容，在不暴露项目方代码情况下查阅，并随时补充到审计框架中。如果赏金或比赛结束后有公开出的优秀审计报告，阅读，总结，并融进框架中


---------------------------------------------------------------

你应该立刻切换成这个“黄金工作流”：
放弃“先通读构建全局模型”的想法，直接进入 “接口-测试”对读的精确打击模式。

1. 列接口（你的攻击面清单）
拿到代码，只做一件事：把生产合约里所有 external 和 public 函数的名字、参数抄出来，形成一张清单。这只需要5分钟。这就是你全部的审计目标。

2. 对每一个接口，执行“三步速射”：
不要读它的实现代码，立刻向下滚动到对应的测试合约（ContractTest）。

· 第一步（秒级理解）： 找对应的测试函数，读它的名字。test_Deposit_Succeeds， test_Withdraw_RevertsIfZero，test_Transfer_UpdatesBalances。
你的收益： 瞬间就知道了这个接口的“核心成功场景”、“核心失败条件”和“预期副作用”是什么。这就是该函数业务规则的浓缩版。

· 第二步（精准验证）： 快速扫一眼这个测试函数里的最后几行断言（assertEq）。
你的收益： 得到了开发者心中这个函数最关键的“状态变化公式”。比如存钱的断言是 assertEq(token.balanceOf(user), 100)，转钱的断言是 assertEq(token.balanceOf(from), 100-initAmount)。
这比读代码快10倍。 你直接从断言里拿到了正确的计算逻辑，然后你再带着这个公式去快速扫一眼合约代码的实现，问一个问题：“代码逻辑在数学上和这个断言逻辑等价吗？” 不等价，就是漏洞。

· 第三步（漏洞挖掘）： 看这个接口的测试，没覆盖什么？
  · 测试只测了整数，你就去测小数或精度丢失。

  · 测试只测了单用户，你就引入第二个用户，看两笔交易交叉会怎样。

  · 测试断言了余额，却没断言 totalSupply 的变化，那 totalSupply 的记账就是你的攻击重点。

3. 那么全局逻辑怎么建立？
就在这个“逐个函数过”的过程中自然建立。
当你用过完 deposit、borrow、repay、withdraw 这一组接口的测试后，你的大脑会无法避免地、自动地把这些精确的“点”连接成一条清晰的“资金流”和“调用链”。这种由精确点连接起来的全局逻辑，比你先去通读代码硬建一个模糊模型，要清晰和可靠得多。












