一、审计核心逻辑

1. 代码，业务能跑通，且注意返回值
1.1 拒绝服务审计 Denial of Service Audit  具体情境：https://chat.deepseek.com/share/7o5s9fjorf38g5rcpd
核心： 由循环与遍历规模过大导致的gas费过大 (或者产生的其它问题，比如上限250kb带来的安全问题，但凡是计算机处理必有上限，所以无限操作一定是有问题)，或传入有误数据(比如传入错误泛型或者错误共享对象ID，或者有问题的外部依赖等等)，或者大速率冲共享对象，总之就是通过冲边界或者错误来破坏交易。所以需要对用户控制的集合强制执行合理的大小上限。在函数开始时或元素添加时进行边界检查。
修复建议示例：
const MAX_SCORES: u64 = 100; // 设定一个合理的上限

public fun process_all_scores_fixed(board: &mut Leaderboard) {
    let len = vector::length(&board.scores);
    // 修复：在执行逻辑前检查集合大小
    assert!(len <= MAX_SCORES, ETooManyScores);
    // ... 后续遍历逻辑 ...
}
修复建议：
    (1) 采用“拉取”模式代替“推送”模式：避免在合约中主动向大量用户“推送”（push）数据（如分红）。应改为让用户自行“拉取”（pull）或认领，每个用户的交易只处理自己的数据。
    (2) 分页处理批量操作：对于必须的批量操作，应引入游标（cursor）或索引（index）参数实现分页处理，确保单次交易处理的条目数在安全范围内。
    (3) 严格限制用户输入长度：对所有用户可控的字符串（String）或字节数组（vector<u8>）字段，在存储到对象之前必须进行长度验证和截断。
    (4) 合理设置数据结构参数：在使用 BigVector 等数据结构时，配置其 max_slice_size 等参数，确保单个节点大小不会超过对象大小限制。
    (5) 设置经济门槛：要求用户在执行可能增加共享状态的操作前，必须存入最低数量的保证金（deposit）或质押品（stake）。这大大提高了攻击者的作恶成本。
1.2 返回值的检查 Unchecked Return Values
核心： 标准库函数或自定义函数设计为 返回布尔值或错误码 来表示成功/失败，而不是直接中断。如果调用方不检查这个返回值，交易仍会继续执行。所以尤其关注所有的函数返回类型，警惕bool，Option<T>和Result<T, E>
1.3 重放攻击
整笔交易重复调用，本质上就是返回值检查，比如在一笔交易完成后并没有进行合适的状态变更(无论成功还是失败都应该做出恰当变更)，导致可以被重复调用。(sui自身的纪元epoch机制提供了防范，sui的交易干燥运行（Dry Run），模拟执行以检查其有效性可以识别失败交易，不会在链上留下失败记录，从而减少攻击风险)
1.4 集合安全：避免灾难性存储溢出
  (1) 已知大小≤1000： 使用VecSet/VecMap
  (2) 位置大小或第三方添加：使用Table/Bag
  (3) 资产存储： 强制使用ObjectBag/ObjectTable
  (4) 禁止在循环中使用vector:push_back处理用户输入
1.5 表键冲突与重复存款
Table是 Sui 提供的一个「键－值」存储结构，它的 add 方法强制要求键必须是唯一的。Bag、ObjectTable、ObjectBag也都是同样道理
漏洞原理：用户第一次存款：键 sender 不存在，add 成功。同一用户第二次存款：sender 这个键已经存在，add 会直接 abort（中止交易），导致用户永远无法追加资金。
所以，任何使用 add 向上述集合插入一个「可能已经存在的键」的时候，都必须先用 contains 检查，或改用 borrow_mut / upsert 等手法。
安全模式通用模板（以 Table 为例）：
if (table::contains(&mut table, key)) {
    let val = table::borrow_mut(&mut table, key);
    *val = *val + new_amount;       //////// 如果已经有key了，那肯定也已经有值了，就在原本值基础上直接加
} else {
    table::add(&mut table, key, new_amount);   /////// 原本没key就add
}


2. 权限，
2.1 访问控制审计 Access Control Audit
核心： 访问权限范围有误 (public, private等)
注意：永远不要使用public(package) entry，因为你public(package)限制了之后又开了个entry接口给用户，那岂不是所有人都可以链接进去，但这种配对在语法上合法，编译器不会报错。应将函数拆分为两个：一个public(package)函数，包含核心逻辑，供包内其他模块调用，并实施严格的权限检查。一个public entry函数，作为交易的入口点，负责权限验证和参数转换，然后调用上面的public(package)函数。不过private函数不需要担心这种问题，因为Sui Move 根本不允许 private entry 这种组合，如果写private entry编译器会直接报错拒绝编译。
2.2 闪电贷代码权限有误
函数可见性与访问控制
  (1) 内部函数是否误用 public 可见性？
  (2) 与资金转移相关的关键函数是否有适当的访问控制（如 sui::tx_context::sender() 校验）？
  (3) 是否存在"查询函数"意外修改状态的情况？Move 本身不区分 view 和写操作，需人工识别。
  (4) entry 函数的参数是否能被攻击者构造出恶意输入？
2.3 权限漏洞审计 Permission Vulnerability Audit
核心： 找清楚接口(public, entry之类)，标注敏感操作(transfer, delete, update, withdraw之类），看清楚所有权，梳理流动
注意： 永远使用 tx_context::sender(ctx) 作为身份验证的唯一依据，不能直接设一个address然后验证address是不是你预设的admin之类，因为这样的话调用者完全可以自己把admin地址传进来。检验的唯一方式就是看tx_context::sender(ctx)是不是符合
修复建议：详情及实际代码https://chat.deepseek.com/share/pn0z9rblpovbxvsjrq
    (1) 存储并验证对象 ID：页面建议在父对象（如 router）中存储所依赖子对象（如 pool）的期望 ID。
    (2) 视所有输入对象为不可信：核心思想是，不要信任任何从外部传入的对象，必须在执行任何状态修改之前，与内部存储的受信任版本 ID 进行比对。
2.4 Ability调用权限
drop并非必需品，drop只是隐式自动销毁，相当于只是一个销毁功能的快捷键，其实际功能完全可以通过直接显式用销毁函数实现，加上drop功能本身就有很大风险被误销毁掉(具体原理看drop功能阐释)，所以对于审计来说，但凡是有drop的地方都值得高度警惕 (可检查可见性)
copy能力虽然在某些场景下属于必需，但仍需高度警惕，审查其逻辑 (可检查可见性) 
2.5 紧急暂停功能 (权限，以及其可能引发的DoS和业务逻辑等问题)
2.6 隐式重入 (本质就是没有设置好访问权限) 具体看https://chat.deepseek.com/share/7pot7e5nmnsf4dtdbv
攻击流程:
start_harvest() -> 保存储备金快照，例如1000。
withdraw_for_strategy() -> 取走500。
start_harvest() -> 再次调用，储备金快照被覆盖为500。
finish_harvest() -> 检查通过（500 >= 500 * 0.98），攻击者成功。

示例代码：
struct HarvestOp { saved_reserves: u64 }

public fun start_harvest(vault: &mut Vault, ctx: &mut TxContext): HarvestOp {
    // 漏洞：没有检查 vault.operation_in_progress 看是否正在strategy交易，就可以反复调用，修复即在这里检查互斥锁，如果已有操作在进行则中止：assert!(!vault.operation_in_progress, EOperationInProgress);
    vault.operation_in_progress = true;
    HarvestOp { saved_reserves: balance::value(&vault.reserves) }
}

public fun withdraw_for_strategy(vault: &mut Vault, _op: &HarvestOp, amount: u64, ctx: &mut TxContext): Coin {
    coin::take(&mut vault.reserves, amount, ctx)
}

public fun finish_harvest(vault: &mut Vault, op: HarvestOp, ctx: &mut TxContext) {
    let HarvestOp { saved_reserves } = op;
    let current = balance::value(&vault.reserves);
    // 攻击者可通过嵌套调用来满足此检查
    assert!(current >= saved_reserves * 98 / 100, EInsufficientReturn);
    vault.operation_in_progress = false;
}

2.7 幽灵角色能力与权限提升
核心：在使用幽灵类型时，必须对类型参数进行严格的“特化”（Specialization）约束。可以仿照public和public(friend)理解，纯粹的<Phantom T>就相当于public，随便传个什么东西进来都可以通过，这样调用者甚至可以自己造一个相符的struct进来运行，所以必须进行特化约束，即类似public(friends)规定谁能进，所以泛型都是需要特化的，毕竟你根本不会有多少情况是允许所有人都进，而且可以看下文，你如果真允许这样了就不需要泛型了，所以但凡是看见有泛型就该查是否有特化，但凡是没有就需要高度警惕，即便是有也需要审查给出的特化是否合理，那同理public(friend)也一样
并且特化可以针对多个对象，实现：https://chat.deepseek.com/share/9t2jq7rxza2zkgeuz0
既然都特化了还选择使用泛型是因为泛型本质上也就是一个快捷键，通常是因为在某些设计中，多个角色会共享大量公共逻辑，比如存储、转移、验证等。用泛型 + 幽灵类型就可以写一份代码处理所有角色，减少重复，但就跟drop和copy一个道理，但凡是快捷键就意味着它舍弃了最安全的做法，那就一定需要高度谨慎，严格约束
而且坦白说，你理解一下泛型，比如说Coin <Phantom T>，前面这个Coin位置的东西，这名字是你自己起的，也就是这地方没有任何内置的东西内置的操作，如果你不特化的话加一个这东西意义在哪呢，前面名字不提供约束，后面T也不提供约束，那就是谁都能进，那如果你要是想谁都能进的话你加这玩意干嘛呢
2.8 类型参数欺骗 Type Parameter Griefing  具体情境与代码及详细阐释：https://chat.deepseek.com/share/ndsqc2cvlrlqrkhu3i
核心：public entry函数中，由于泛型类型参数的具体类型由调用者在调用时（call site） 决定，而非合约定义时预设，所以如果在泛型检验之前进行了不可逆操作(如pop、delete、transfer)，不可逆操作将成功执行，因为此时还未进行验证，这样即便之后的验证返回false也已经无力回天
修复建议：其实本质上这和2.7形成互补，2.7可以作为这里的修复建议，但这里又提供了更新更全面的解决方案
    (1) 验证在状态变更之前：在执行任何不可逆的状态变更（如pop, delete, transfer）之前，必须对泛型类型参数进行严格验证，确保其与预期的资产类型匹配。
    (2) 使用 assert! 立即终止：在类型不匹配时，不要返回false或静默跳过，而应立即使用 assert! 中断执行并抛出错误码。这样整个交易会回滚，状态不会受损。
    (3) 设置前置“守卫”函数：将类型验证逻辑抽离成一个独立的守卫函数，并确保在代码逻辑上必须先通过该守卫，才能执行后续的破坏性操作。这从代码结构上杜绝了“先操作后验证”的可能。
    (4) 考虑Phantom类型参数：
        幻影类型参数结合见证人模式（Witness Pattern），可以在对象创建时就将其与特定类型“绑定”，后续调用函数时，类型必须在编译时匹配，否则无法通过编译。这从根本上杜绝了调用者在运行时任意替换类型的可能。
        见证人模式提供了更灵活的使用，可以给任意具体类型提供普适的使用方法。其本质原理和2.7中的特化一样，特化是通过&RoleCap<ModRole>在输入中强制要求输入具体类型，而见证人模式则是不在合约中指明具体类型，合约只提供泛型，然后由资产部署者自己写清楚具体资产模块，这样的好处是同一个合约可以服务于无数个资产部署者，而不需要为每一个资产部署者提供合约，这样不管是占用链上的空间还是可能存在的修改之类都更优
        具体代码：https://chat.deepseek.com/share/m25rflq1b87jvnp418


3. 所有权，
3.1 对象管理审计 Object Management Audit
核心： 对象类型可能不对
3.2 Token 代币消耗审计 Token Consumption Audit
核心： coin是object，注意它金额，存在与所有权等是否按照预期进行
3.3 缺失对象唯一性检查 Missing Object Uniqueness Audit
核心：Sui 的对象模型虽然为每个对象分配了全局唯一的 UID，但缺乏"每个地址只能拥有一个特定类型对象"的内置约束。这意味着，如果没有额外的检查，一个用户地址可以持有多个同类型的对象（例如多个 Membership 对象），从而破坏协议中"一个用户一个会员资格"的业务逻辑假设。(当然，这需要根据实际业务逻辑判断，肯定有业务就需要需要同时拥有多个)
修复方案：
    (1) 方案一：使用 Table 或 ObjectTable 注册
    (2) 方案二：使用动态字段
    (3) 方案三：使用派生对象（Derived Objects）
具体情况：https://chat.deepseek.com/share/9zqfkqtyplrx7008zf
3.4 可变引用遮蔽 Mutable Reference Shadowing  具体及代码示例：https://chat.deepseek.com/share/trm0x2kwnw5z4t8dw1
核心：在使用 mut 关键字解构结构体的可变引用时，你会得到一个新的局部变量，而不是一个指向原字段的别名，直接为其赋值无法修改链上状态，所以想要修改引用所指向的数据，必须使用解引用 * 运算符，如 *left = *limit;。这能明确地一次完成两个操作：先通过 * 读出新变量的值，再将其写入原结构体的字段。
修复方法非常直接，就是对赋值操作的两边都进行解引用。
修复方案：
编码规范：
    (1) 时刻使用解引用：从可变引用解构出字段后，任何写操作都必须使用 *。field = value 是本地变量绑定，*field = value 才是透过引用写入。
    (2) 优先直接赋值：如果 Move 版本支持，更推荐直接对结构体引用进行字段赋值，如 cap.left = cap.limit;，它能从语法层面彻底规避遮蔽问题。



4. 时间差，
4.1 条件竞争审计 Race Conditions Audit
核心：利用时间差抢跑，可能出在调用合约前中后三个阶段，本质上是因为PTB内部指定了指令顺序，但PTB内相邻指令之间并非即时连接，而是分别进入各自的队列排队，这也就导致了前一个指令结束之后，后一个指令需要进入队列，而从此时开始到该指令被执行，你要操作的对象可能已经被队列前方的指令做了巨大改变
所以这本质上是sui链的系统性问题，唯一能做的只有两点，一，设置滑点，二，尽量减少共享对象。如果出现共享对象，由审计师根据具体情况判断风险
4.2 跨链桥
4.3 价格预言机操纵 (大资金冲，比如闪电贷)
  (1) 是否使用时间加权平均价格（TWAP）而非瞬时价格？
  (2) 是否依赖单一 AMM 池作为唯一价格来源？单一来源极易被闪电贷临时操纵。
  (3) 是否结合了多个预言机来源（如 Pyth、Switchboard 等 Sui 生态原生预言机）？
  (4) 是否实现了价格偏离阈值检测与熔断机制（超过阈值则拒绝执行）？


5. 任何交互过程可能产生的逻辑错乱和增加的攻击面
5.1 Randomness攻击
随机数只有在模块内部生成才确保安全，因为随机都是伪随机，本质只是算法，如果允许外部操作则外部可以计算出随机数
https://chat.deepseek.com/share/tprqbqlfkpr42b4vmb
https://chat.deepseek.com/share/qm5al1hp8zm0fbxcnn
https://chat.deepseek.com/share/kqzo4zloke4bmu2xc5
5.2 合约升级的安全审计 Smart Contract Upgrade Security Audit
核心：是否成功把旧版本换成新版本
核心总结：
审计项目	          关键问句	                                             常见高危信号
初始化逻辑	      init 中的代码是否在升级后通过迁移函数重新执行？	         升级后运行报 Object not found。
依赖更新	          引用的外部包地址是否已过时且存在已知漏洞？	             Move.toml 中依赖项未更新 rev。
动态字段迁移	      是否检查了所有 dynamic_field Key 的命名空间变更？	     旧 Key 直接丢弃，数据丢失。
对象 ID 稳定性	  迁移后关键对象的 ID 是否改变？	                         硬编码 ID 的下游服务停止工作。
防重放保护	      迁移函数是否包含 assert! 确保仅执行一次？	             可被反复调用刷取奖励或导致状态混乱。

经典漏洞：验证器绕过漏洞 Verifier Bypass via Package Upgrade
核心：当你在 Sui 上升级一个合约（包），并不是把原来的代码覆盖掉，而是发布一个全新的包，这个新包装着修改后的代码。原来的旧包（V0）和它的所有函数，仍然保留在链上，不会删除。
而 可编程交易块（PTB） 是一个很强大的功能：它允许你把多个操作打包成一笔原子交易。在这笔交易里，你可以：先调用旧版本 V0 的一个函数，拿到一个结果。紧接着，再调用新版本 V1 的一个函数，把刚才的结果传进去。
key 能力：在 Sui Move 中，如果一个结构体（struct）拥有 key 能力，它就被视为一个可以独立存储和转移的 Sui 对象。它的第一个字段必须是 id: UID，作为一个独一无二的身份标识。
id_leak_verifier：Sui 的字节码验证器会检查所有拥有 key 能力的结构体，确保其 id 字段必须来自 object::new(ctx) 函数。这个检查是为了防止出现对象身份伪造（Object Identity Spoofing）。
核心矛盾：问题在于，这个检查仅针对当前拥有 key 能力的结构体。如果一个结构体在被声明时没有 key，验证器就会认为它不是 Sui 对象，从而允许它打包任意的 UID，而不会触发 id_leak_verifier 的检查。
所以你就可以在前后两个版本分别构造带key和不带key的同名struct，这样你就能把不带key结构体传到带key结构体的函数里面操作
实例：https://chat.deepseek.com/share/cpq4t6ajybqgs1xp5u



5.3 外部调用安全审计 External Call Function Security Audit
核心： 外部模块稳定性与可信度， 访问权限有误， 复杂调用产生的问题(过程中产生的时间差，错误调用，以及递归调用导致的资源耗尽)
5.4 Gas 优化审计 Gas Optimization Audit
核心： 不影响功能情况下越简单越好(比如把链上操作搬到链下做，再传结果回来，这样可以简化链上流程，但这过程中产生的交互和时间差本身也可能成为被攻击的对象，所以需充分检查)



6. 数学逻辑正确性： 
    6.1 溢出审计 (包括大资金冲，比如闪电贷)
      6.1.1 位运算溢出
      6.1.2 自定义函数溢出
        https://chat.deepseek.com/share/2nse699ycb6rtvhfuz
        6.1.2.1 流动性计算公式、费率计算等核心业务逻辑是否存在数值边界漏洞？
        6.1.2.2 是否有极端参数输入（如极大 tick、极小流动性区间）可以触发意外行为？
    6.2 算术精度误差审计 Arithmetic Accuracy Deviation Audit
    核心： 十进制只有整数没有小数，小数部分会被砍掉，所以有误差
    修改建议：https://chat.deepseek.com/share/o4vdv0xjxu842m1yen
    6.3 单位统一
    时间、金额等，需统一，而且命名也需要和实际单位同步，否则如果实际单位和你以为的单位不一样很可能就会出灾难
    如：关键函数: clock::timestamp_ms(clock): 返回自Unix纪元以来的毫秒数。
    常量STAKE_LOCK_TIME_SECONDS: 虽然在名字里标明了秒，但它在代码中直接被用作一个数值。
    漏洞原理：stake函数将毫秒值存入了名为seconds的字段。而unstake函数在验证时，错误地将毫秒值与秒值进行了比较，导致锁定时间被万倍缩短。
    解决方案：直接在最开始乘个系数把单位统一即可，不过既然在合约中没有具体值，具体是什么单位基本依靠对不同内置函数的默认返回单位了解，先验了解所有这种函数大海捞针而且效率很低，不如每次审计时着重关注所有带单位的东西，凡是会返回它们的函数都单独查阅会返回什么单位

7. 实际效果和预想的业务逻辑是不是匹配
设计逻辑审计 Design Logic Audit
核心： 梳理逻辑，控制权限与所有权，对比实际情况与业务设计

              
8. 事件日志审计                                    
关键操作是否记录事件


二、工具使用： 总使用教程：https://chat.deepseek.com/share/96u1h3sz9xxh4kn75w  工具理解：https://chat.deepseek.com/share/envjawutqxvx1zlded  流量限制、保密等条款约束：https://chat.deepseek.com/share/x4bpe6tlimp36vxfzm
1. 跑得飞快的“规则专家”：静态分析 (Static Analysis)
这就是你担心的“模板匹配”的典型。它不运行代码，而是将合约源码转换成一个叫 SlithIR 的中间表示（IR），然后像高级的代码检查工具（Linter）一样，去匹配已知的漏洞模式（比如检查你是否使用了不安全的 tx.origin 做鉴权），或者通过构建控制流图（CFG）分析复杂的调用关系。它的优势是快（秒级出结果），非常适合在开发早期快速筛查代码风格和结构性问题。但局限也很明显：无法深入检测复杂的业务逻辑，且存在一定的误报。
  (1) Sentri：快速的多链静态检查器（支持Move），可在数秒内给出反馈。
      CLI 命令：npx @sentri/cli check ./sources --chain move
      链接：https://www.npmjs.com/package/@sentri/cli

  (2) Sui-Move-Analyzer：Team BitsLab (MoveBit) 开发的集成开发插件，与 VSCode 等融合，强化了代码导航、自动补全和框架集成。
      链接：https://movebit.xyz/blog/post/Introducing-Sui-Move-Analyzer-Development-Made-Simple.html

  (3) MoveMaker：有项目基于此构建了合约审计平台，旨在分析包括 Move 在内的多种语言。
      链接：https://github.com/A1LinLin1/hackathon


2. 抽丝剥茧的“数学侦探”：符号执行 (Symbolic Execution)
这是为了弥补静态分析“不够深”而生的技术，以 Mythril 工具为代表。程序执行时，正常输入是具体的值（如 amount = 100），但符号执行会用符号（X）代替具体值。它会分析 amount = X 时，程序后续每个条件分支（If/Else/Switch），并为每条路径生成一个由约束条件组成的数学公式，比如“如果X > 0 且 Y < 100...”。“实际运行” 这一步就发生在这里，工具会尝试求解这个数学公式，看是否存在一种情况能让攻击发生。它能像侦探一样问：“是否存在任何一个 X 值，能触发这个漏洞？”
它的“推理”本质是数学求解。它把你代码逻辑变成方程，然后问 SMT 求解器：“能解出一个 x 让 balance 变成负数吗？” 这完全不是 AI 式的理解，而是机械的、冷冰冰的逻辑计算。

Move Bytecode Symbolic Execution Engine：专为 Move 字节码设计的推理引擎。它会将可能的漏洞路径转化为方程，通过约束求解器 Z3 寻找可行解。
GitHub：https://github.com/publicqi/pbctf23-move-vm


3. 随机应变的“探索者”：模糊测试 (Fuzzing)
另一种你提到的“先运行一下看结果”的技术是Fuzzing，其代表工具是 Echidna。它会用海量的、伪随机的数据去“轰炸”你的合约函数，尝试触发你预先定义好的安全属性（例如“永远不能超提”)。Fuzzing的可靠性很高，它发现的都是真实存在的漏洞，几乎没有误报。它不推理，但它用暴力证明，如果跑了 1 亿次都没破，那这个属性大概率是成立的。
  (1) Sui-fuzzer (by FuzzingLabs)：与 Sui 基金会合作开发。支持通过子模块快速集成，只需指定已编译模块路径和目标函数名即可运行。
      GitHub：https://app4.secure.forcepoint.com/FuzzingLabs/sui-fuzzer

  (2) Belobog：由 MoveBit 团队研发的“类型感知”框架，极大解决了在 Move 强类型体系下生成有效测试用例的难题。评测显示它能发现人工审计中 100% 的严重漏洞和 79% 的主要漏洞，并已成功复现了真实攻击事件。
      Paper：https://arxiv.org/abs/2512.02918

  (3) MoveFuzz：专注于 Sui Move 智能合约的字节码级模糊测试工具。由Fuzzland团队开发，支持本地和链上部署，特点在于误报/漏报少且支持多交易攻击检测
      报道：https://www.bitpush.news/articles/4147130
      Github：https://github.com/Movebase/sui-fuzzer


4. 终极武器：形式化验证 (Formal Verification)
如果前几种是“找错”，那形式化验证就是“证真”。它旨在用数学方法证明，你的合约在所有可能的输入和状态下，100%符合你的规范。这就像做数学证明题，一旦得证，就意味着某些类型的漏洞被完全杜绝了。近年来，一些代表性工具使得这项曾经高精尖的技术开始变得民主化和开发者友好。
  (1) Certora Sui Prover：强大的商业级工具，使用 CVLM 语言编写规范。可免费注册账户并在 CI/CD 中集成，能证明函数前置/后置条件和关键属性。（需要联网，但不上传源码
，安全上来说，这是一个云端验证服务。你用 Certora 的 CLI 在本地编写规范文件（.spec 文件），然后运行 certoraSuiProver.py。它会将你的合约源码和规范上传到 Certora 的云端服务器进行求解。Certora 是企业级服务，有严格的保密协议，服务过 Aave、MakerDAO 等头部协议，但仍需你自行评估合规性。）
      文档：https://docs.certora.com/

  (2) Sui Prover (by asymptotic-code)：开源方案，基于 Boogie 引擎和 Z3 求解器，支持 brew install。直接在合约中编写 spec 块并运行 sui-prover 命令即可。
      GitHub：https://github.com/asymptotic-code/sui-prover


5. AI作为增强 (AI-Enhanced)：将AI驱动的审计工具或CI/CD流程中的自动扫描，作为人工审计前“降噪”和提示的环节，帮助人类审计师锁定可疑点，大幅提升最终人工审计的综合效率
  (1) BitsLabAI Scanner：Team BitsLab 打造的 AI 审计代理。在 Sui 的审计大赛中，它的表现曾超越多数参赛审计员并获得第二名，展示了强大的自动漏洞检测和上下文分析能力。（目前以网页上传源码的方式使用，你的代码会被发送到他们的服务器进行 AI 分析。需要信任 MoveBit 团队的数据处理政策。）
      了解更多：https://movebit.xyz/

  (2) SuiGPT MAD：由卡内基梅隆大学团队开发的LLM反编译工具，能将 Sui 上未开源的字节码反编译为可读代码，重编译成功率73.33%，极大提升了透明度和可审计性。（你需要在网页上粘贴字节码才能反编译。字节码本身已经是公开的（链上数据），但如果你粘贴的是未开源项目的私有字节码，同样有泄露风险。）
      论文：https://arxiv.org/abs/2410.15275
      链接：https://suigpt.tools/mad

  (3) M.I.E.S.C.：一个多层安全分析框架，通过编排 50 多种安全工具并结合 AI/ML 检测，覆盖9层防御的预审计分诊框架，作为审计前分类筛选工具，主要面EVM链。它对Move的支持目前仍处于Alpha阶段。
      API：https://pypi.org/project/miesc/
      Github：https://github.com/fboiero/MIESC

6. 补充
Gas费估算工具
工具/资源	                    |  用途
sui client dry-run	          |  模拟交易，预估 Gas 成本
Sui Move Analyzer（≥0.8.7） 	|  VS Code 中实时显示存储成本预测
Sui Explorer Gas Tracker	    |  实时比照实际 Gas 消耗，误差应在 ±3% 内
Devnet / Testnet 环境	        |  估算应用 Gas 费用、计算初始资本需求


三、测试验证

1. 单元测试： 覆盖边界条件
2. 覆盖率测试： 消灭盲点
3. 模糊测试： 发现隐藏漏洞
4. 压力测试


四、安全发布流程
Step1: 测试网部署
Step2: 监控72小时
Step3: 异常检测
if result == no
  Step4: 审计通过
  Step5: 主网分阶段部署
  Step6: 1%流量测试
  Step7: 50%流量测试
  Step8: 100%部署
else
Step4: 回滚修复


五、链上监控与响应 
1. 关键指标监控
  (1) 异常交易频率
  (2) 权限变更事件
  (3) 余额异常波动
  (4) 函数调用失败率

2. 应急响应流程
Step1： 触发紧急暂停
Step2： 分析攻击路径
Step3： 部署修复版本
Step4： 执行资产恢复
Step5： 发布事后报告

