1. 溢出审计 Overflow Audit
说明：

Move 进行数学运算时会进行溢出检查，运算溢出交易将失败。但需要注意的是位运算并不会进行检查。此外，自定义溢出检测函数可能存在缺陷，导致数值截断问题。

定位：

寻找代码中进行位运算的位置，检查是否有溢出风险。
检查自定义溢出检测函数的阈值设置和边界条件判断。
验证位移操作后是否会超出目标类型最大值而被截断。
示例：

// 错误示例 
public fun checked_shlw(n: u256): (u256, bool) {    // (u256, bool) 表达返回值必须按照 (数字，布尔判断) 的形式呈现，就类似一个向量 // u多少就表示盒子是二进制多少位
    let mask = 0xffffffffffffffff << 192;  // ❌ 错误的掩码 任何小于 `0xffffffffffffffff << 192` 的值都可以通过  // f = 1111 // <<192 即左移192位
    if (n > mask) {                         // ❌ 错误的判断条件
        (0, true)
    } else {
        ((n << 64), false)                  // ⚠️ 左移溢出被截断  // u多少规定了盒子大小，即一共可以容纳的二进制位数，如果在左移过程中超出了最左侧的位数边界那信息就丢失，即溢出截断
    }
}

Move 不会像 Solidity 那样，因为一个简单的 + 或者 - 就把你的钱算成天文数字（下溢变极大）或者归零（上溢变极小）。

在 Move 里，普通算术溢出的结果是直接报错、交易回滚、一切恢复原样。攻击者捞不到一分钱，还要倒贴 Gas 费。

但是，位运算截断攻击的收益点不在于“让交易不报错”，而在于“让交易静默通过，但逻辑结果完全偏离预期”。

作恶者的好处通常体现在以下三个高价值场景：

场景一：偷工减料的金额计算（最常见的作恶）
假设一个 DeFi 协议为了省 Gas，自己写了一个“安全乘法库”（就像你看到的那段示例代码）。

正常逻辑：存 100 个币，计算利息时乘以 1.5 倍的系数。

作恶者输入：精心构造一个极大的 n，使得左移 64 位后高位被砍掉，剩下的部分变成了一个极小的数字（比如 1）。

结果：

算术检查：通过（因为位运算不检查）。

最终计算出的金额：本来该给你 150 个币，现在变成了 0.000001 个币。

作恶者收益：他通过正常的交易流程，利用逻辑漏洞窃取了本该属于别人的利息差，或者用极小的抵押借走了极大的资产。

场景二：权限校验的绕过（Bitmask 掩码错误）
很多合约用位运算来管理权限，比如：

第 1 位代表：能不能转账。

第 2 位代表：能不能销毁。

move
// 开发者写了一个错误的位检查函数
fun has_permission(flags: u256, permission_bit: u8): bool {
    // 这里本意是检查 flags 的第 N 位是不是 1
    // 但位移溢出导致检查错位了
}
作恶者操作：传入一个特定的 flags，利用位移截断让检查函数明明没有权限却返回 True。

结果：普通用户突然获得了管理员权限，可以提走池子里的钱。

场景三：与传统 0-1 溢出的根本差异对比
为了让你更清楚区别，看这个对比表：

维度	     | 传统 Solidity 0-1 下溢	          | Move 位运算截断攻击
报错机制	     | 不报错，默默变成 uint256 最大值	  | 不报错，默默把高位砍掉
作恶原理	     | 利用编译器/虚拟机底层缺陷改写数值	  | 利用开发者自定义检测函数的逻辑缺陷
直接后果	     | 余额直接变 凭空印钱	              | 计算结果被篡改，业务逻辑发生偏移
攻击者收益	 | 瞬间暴富，直接提走所有钱	          | 窃取差价、绕过权限、低价清算资产
总结：作恶者的“好处”到底是什么？
在 Move 中，攻击者通过位运算截断得到的“好处”不是 “1 变成了 1 亿” ，而是：

“我明明输入了 X，代码告诉我结果是 Y，但实际存进数据库/发出去的资产是 Z。”

只要 X ≠ Y ≠ Z，并且这个偏差没有被 Move 虚拟机捕获（因为虚拟机认为位运算截断是合法的语法），攻击者就能利用这个偏差套利。

一句话总结：
Solidity 溢出是 机器算错了；
Move 位运算截断是 程序员逻辑写错了，但机器觉得你没错。

逻辑错误导致的问题，只需改正，无需防御代码

------------------------------------

2. 算术精度误差审计 Arithmetic Accuracy Deviation Audit
说明：

Move 中没有浮点类型。因此，在进行算术运算时，如果运算结果需要以浮点数表示，可能会产生精度误差。虽然精度误差在某些情况下难以完全避免，但可以通过优化和合理设计来减轻其影响。

定位：

审查代码中所有涉及算术运算的部分，特别是可能产生精度误差的计算，确保这些运算不会对合约逻辑或数值准确性产生负面影响，并提出优化建议以减轻精度误差。

详细阐释： https://chat.deepseek.com/share/o4vdv0xjxu842m1yen

-------------------------------------

3. 条件竞争审计 Race Conditions Audit
说明：

Sui 中的验证者也可以对用户提交的交易进行排序，因此在审计中我们仍需要注意在同一个区块中对交易进行排序而进行获利的问题。

定位：

(1) 是否有对函数调用前合约的数据状态进行预期管理。
翻译：函数执行前，合约先看了一眼数据（比如余额、开关状态），然后基于这个数据做决策。但在看和执行之间，别人可能把数据改了。

经典漏洞例子：抢跑提款

move
// 用户调用 withdraw(amount)
// 函数先查余额：balance >= amount ？ 通过 ✅
// 但在执行转账前，另一笔交易把余额转走了
// 结果函数仍然按旧余额执行转账，导致超额提款
审计要问：你依赖的“调用前状态”会不会在同一个区块里被别的交易提前改掉？


(2) 是否有对函数执行中的合约数据状态进行预期管理。（Sui 特有）
翻译：函数不是原子瞬间完成的，在它执行中途，状态可能因其他交易介入而改变（尤其在 Sui 的对象模型中，如果是多个独立对象操作，中间状态可能暴露）。

Sui 特有问题：Sui 使用对象锁，但如果你在一个 PTB（可编程交易块）里操作多个无关联的对象，这些操作之间没有全局锁，验证者可能在中间插入别人的交易。

审计要问：你假设执行过程中某数据保持不变，但实际上它可能被改了吗？

这是 Sui 的 可编程交易块（PTB） 和 无全局锁 带来的特有现象。

具体原理：

在 Sui 里，一笔交易可以操作多个对象。假设一笔交易包含两个命令：

修改对象 A

修改对象 B

关键机制：如果 A 和 B 不共享同一个所有者（例如 A 是共享对象，B 是用户独享对象），Sui 的并行执行引擎会在 命令1 和 命令2 之间的纳秒级空隙中，允许其他无关的交易插入执行。

大白话场景（插队修车）：

预期：你先换轮胎（A），再换机油（B），你想当然地认为换完轮胎后，车子底盘高度没变，方便你放机油。

现实（Sui）：你换完轮胎（A），千斤顶还没放下来呢，隔壁老王钻进来把你千斤顶撤了换了个更高的（其他交易修改了全局状态/预言机价格）。

结果：等你执行换机油（B）时，车子倾斜了，机油洒了一地。

审计代码示例逻辑：

move
// 假设这是一个兑换函数，分两步操作
public entry fun swap_bad(ptb: &mut TxContext) {
    // 步骤 1：从共享池 X 拿钱 (此时池子比例是 1:1)
    let coin_out = pool::withdraw_share<X>(...); 
    
    // ⚠️ 注意：这里是一个潜在的“气隙”
    // 验证者可能在这里插入一笔交易，去另一个池子 Y 套利，导致池子 Y 失衡
    
    // 步骤 2：把拿到的钱存入池子 Y (此时池子 Y 比例可能已经变了)
    pool::deposit_share<Y>(coin_out, ...); 
}

(3) 是否有对函数调用后的合约数据状态进行预期的管理。（并行写入冲突）
翻译：函数执行完，你觉得状态已经更新到某个值了，并且依赖这个新状态做后续操作（比如同一笔交易后面的步骤，或者下一笔交易）。但如果别人立刻发起一笔交易，在你期望的“新状态”生效前就冲进来操作，可能出问题。

例子：价格预言机更新后，套利者立刻反向操作。

审计要问：你对“调用后的世界”的假设是否会被下一笔紧跟着的交易打乱？需不需要增加时间锁或冷却期？

这是 Sui 并行执行 下最容易忽略的盲区。

具体原理：
你的函数执行完，把结果写回链上对象了。你假设这个对象从现在起就是“锁死”的、或者处于某种“中间态”等待你下一笔交易来认领。但在 Sui 的共识层，验证者可以在你的两笔交易之间塞入成千上万笔交易。

大白话场景（打地鼠）：

预期：你操作完 game.move，状态变成 GameEnded，你心想：下一笔交易我就来领奖，这地鼠洞已经堵上了，没人能动了。

现实（Sui）：你刚写完 GameEnded，下一个时间片（可能只有 200ms），验证者立刻打包了一笔交易又把状态改成了 GameStarted（比如有人续费了，或者重置了）。

结果：你的下一笔领奖交易执行时，读到的状态是 GameStarted，导致领奖失败或逻辑错乱。

Sui 审计中的“调用后预期”特指：

事件（Event）依赖：你发了个 Event 觉得链下服务会处理，但在链下服务处理前，状态又被别人改了。

Hot Potato 模式：你返回了一个特殊的“收据”对象（必须立刻被下一笔交易消费），但在你构造下一笔交易的 RPC 延迟里，另一个交易强行创建了一个一模一样的“收据”并抢先消费了。

因为 Sui 验证者可以给交易排序，所以合约不能天真地相信“我看数据时是这样，执行时就一定还是这样”。审计就是检查：调用前看的数、执行中依赖的数、调用后期待的数，这三个阶段的数据一致性假设是否会被恶意顺序打破。

进一步阐释（包括防御代码）： https://chat.deepseek.com/share/cbz1vckrdqvnc7khux

4. 访问控制审计 Access Control Audit
说明：

合约中的某些关键函数应仅限于内部调用，例如能够直接更新用户存款数额的函数。如果这些函数不慎对外部开放，可能绕过权限控制，导致安全漏洞，甚至引发资产损失。因此，必须严格设置访问权限，确保只有授权角色或模块可以调用这些函数，或者确保函数只能在内部使用。

定位：

需要检查所有函数的访问控制设置，特别是那些不应该对外公开的函数，确保它们只能在内部调用。如果发现不应暴露的函数接口已经公开，必须标记为高风险并提出修正建议。
