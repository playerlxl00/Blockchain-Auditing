核心逻辑：
第一，代码，业务能跑通，
第二，权限，
第三，所有权，
第四，时间差，
第五，任何交互过程可能产生的逻辑错乱和增加的攻击面
第六：数学逻辑正确性： https://chat.deepseek.com/share/2nse699ycb6rtvhfuz

注意：PTB打包操作是在前端进行，进入合约的已经是PTB本身


0.1 重入攻击/双花
同一笔钱在不同交易中重复花
https://chat.deepseek.com/share/5z1dcicmb5uf9eyxz3
https://chat.deepseek.com/share/qm5al1hp8zm0fbxcnn

0.2 重放攻击
整笔交易重复调用
https://chat.deepseek.com/share/57d2r5k3d46auykurt

0.3 Randomness攻击
随机数只有在模块内部生成才确保安全，因为随机都是伪随机，本质只是算法，如果允许外部操作则外部可以计算出随机数
https://chat.deepseek.com/share/tprqbqlfkpr42b4vmb
https://chat.deepseek.com/share/qm5al1hp8zm0fbxcnn
https://chat.deepseek.com/share/kqzo4zloke4bmu2xc5

0.4 溢出攻击 (主要针对传统ethereum，关于sui的溢出会在后文详细说明)
0-1攻击
https://chat.deepseek.com/share/qm5al1hp8zm0fbxcnn


1. 溢出审计 Overflow Audit
说明：

Move 进行数学运算时会进行溢出检查，运算溢出交易将失败。但需要注意的是位运算并不会进行检查。此外，自定义溢出检测函数可能存在缺陷，导致数值截断问题。

定位：

· 寻找代码中进行位运算的位置，检查是否有溢出风险。
· 检查自定义溢出检测函数的阈值设置和边界条件判断。
· 验证位移操作后是否会超出目标类型最大值而被截断。
示例：

// 错误示例 
public fun checked_shlw(n: u256): (u256, bool) {    // (u256, bool) 表达返回值必须按照 (数字，布尔判断) 的形式呈现，就类似一个向量 // u多少就表示盒子是二进制多少位
    let mask = 0xffffffffffffffff << 192;  // ❌ 错误的掩码 任何小于 `0xffffffffffffffff << 192` 的值都可以通过  // f = 1111 // <<192 即左移192位
    if (n > mask) {                         // ❌ 错误的判断条件
        (0, true)
    } else {
        ((n << 64), false)                  // ⚠️ 左移溢出被截断  // u多少规定了盒子大小，即一共可以容纳的二进制位数，如果在左移过程中超出了最左侧的位数边界那信息就丢失，即溢出截断
    }
}

Move 不会像 Solidity 那样，因为一个简单的 + 或者 - 就把你的钱算成天文数字（下溢变极大）或者归零（上溢变极小）。

在 Move 里，普通算术溢出的结果是直接报错、交易回滚、一切恢复原样。攻击者捞不到一分钱，还要倒贴 Gas 费。

但是，位运算截断攻击的收益点不在于“让交易不报错”，而在于“让交易静默通过，但逻辑结果完全偏离预期”。

作恶者的好处通常体现在以下三个高价值场景：

场景一：偷工减料的金额计算（最常见的作恶）
假设一个 DeFi 协议为了省 Gas，自己写了一个“安全乘法库”（就像你看到的那段示例代码）。

正常逻辑：存 100 个币，计算利息时乘以 1.5 倍的系数。

作恶者输入：精心构造一个极大的 n，使得左移 64 位后高位被砍掉，剩下的部分变成了一个极小的数字（比如 1）。

结果：

算术检查：通过（因为位运算不检查）。

最终计算出的金额：本来该给你 150 个币，现在变成了 0.000001 个币。

作恶者收益：他通过正常的交易流程，利用逻辑漏洞窃取了本该属于别人的利息差，或者用极小的抵押借走了极大的资产。

场景二：权限校验的绕过（Bitmask 掩码错误）
很多合约用位运算来管理权限，比如：

第 1 位代表：能不能转账。

第 2 位代表：能不能销毁。

move
// 开发者写了一个错误的位检查函数
fun has_permission(flags: u256, permission_bit: u8): bool {
    // 这里本意是检查 flags 的第 N 位是不是 1
    // 但位移溢出导致检查错位了
}
作恶者操作：传入一个特定的 flags，利用位移截断让检查函数明明没有权限却返回 True。

结果：普通用户突然获得了管理员权限，可以提走池子里的钱。

场景三：与传统 0-1 溢出的根本差异对比
为了让你更清楚区别，看这个对比表：

维度	     | 传统 Solidity 0-1 下溢	          | Move 位运算截断攻击
报错机制	     | 不报错，默默变成 uint256 最大值	  | 不报错，默默把高位砍掉
作恶原理	     | 利用编译器/虚拟机底层缺陷改写数值	  | 利用开发者自定义检测函数的逻辑缺陷
直接后果	     | 余额直接变 凭空印钱	              | 计算结果被篡改，业务逻辑发生偏移
攻击者收益	 | 瞬间暴富，直接提走所有钱	          | 窃取差价、绕过权限、低价清算资产
总结：作恶者的“好处”到底是什么？
在 Move 中，攻击者通过位运算截断得到的“好处”不是 “1 变成了 1 亿” ，而是：

“我明明输入了 X，代码告诉我结果是 Y，但实际存进数据库/发出去的资产是 Z。”

只要 X ≠ Y ≠ Z，并且这个偏差没有被 Move 虚拟机捕获（因为虚拟机认为位运算截断是合法的语法），攻击者就能利用这个偏差套利。

一句话总结：
Solidity 溢出是 机器算错了；
Move 位运算截断是 程序员逻辑写错了，但机器觉得你没错。

逻辑错误导致的问题，只需改正，无需防御代码

拓展代码： https://chat.deepseek.com/share/rfr18sv0pslwdwolbo

------------------------------------

2. 算术精度误差审计 Arithmetic Accuracy Deviation Audit
核心： 十进制只有整数没有小数，小数部分会被砍掉，所以有误差

说明：

Move 中没有浮点类型。因此，在进行算术运算时，如果运算结果需要以浮点数表示，可能会产生精度误差。虽然精度误差在某些情况下难以完全避免，但可以通过优化和合理设计来减轻其影响。

定位：

审查代码中所有涉及算术运算的部分，特别是可能产生精度误差的计算，确保这些运算不会对合约逻辑或数值准确性产生负面影响，并提出优化建议以减轻精度误差。

详细阐释(优化，无防御代码)： https://chat.deepseek.com/share/o4vdv0xjxu842m1yen

-------------------------------------

3. 条件竞争审计 Race Conditions Audit
核心：利用时间差抢跑，可能出在调用合约前中后三个阶段，本质上是因为PTB内部指定了指令顺序，但PTB内相邻指令之间并非即时连接，而是分别进入各自的队列排队，这也就导致了前一个指令结束之后，后一个指令需要进入队列，而从此时开始到该指令被执行，你要操作的对象可能已经被队列前方的指令做了巨大改变
所以这本质上是sui链的系统性问题，唯一能做的只有两点，一，设置滑点，二，尽量减少共享对象。如果出现共享对象，由审计师根据具体情况判断风险
PTB机制详细阐释以及关于条件竞争审计讨论： https://chat.deepseek.com/share/3frbfzjjnji26acwge

说明：

Sui 中的验证者也可以对用户提交的交易进行排序，因此在审计中我们仍需要注意在同一个区块中对交易进行排序而进行获利的问题。

定位：

(1) 是否有对函数调用前合约的数据状态进行预期管理。
翻译：函数执行前，合约先看了一眼数据（比如余额、开关状态），然后基于这个数据做决策。但在看和执行之间，别人可能把数据改了。

(2) 是否有对函数执行中的合约数据状态进行预期管理。（Sui 特有）
翻译：函数不是原子瞬间完成的，在它执行中途，状态可能因其他交易介入而改变（尤其在 Sui 的对象模型中，如果是多个独立对象操作，中间状态可能暴露）。


(3) 是否有对函数调用后的合约数据状态进行预期的管理。（并行写入冲突）
翻译：函数执行完，你觉得状态已经更新到某个值了，并且依赖这个新状态做后续操作（比如同一笔交易后面的步骤，或者下一笔交易）。但如果别人立刻发起一笔交易，在你期望的“新状态”生效前就冲进来操作，可能出问题。


-----------------------------------------------

4. 访问控制审计 Access Control Audit
核心： 访问权限范围有误

说明：

合约中的某些关键函数应仅限于内部调用，例如能够直接更新用户存款数额的函数。如果这些函数不慎对外部开放，可能绕过权限控制，导致安全漏洞，甚至引发资产损失。因此，必须严格设置访问权限，确保只有授权角色或模块可以调用这些函数，或者确保函数只能在内部使用。

定位：

需要检查所有函数的访问控制设置，特别是那些不应该对外公开的函数，确保它们只能在内部调用。如果发现不应暴露的函数接口已经公开，必须标记为高风险并提出修正建议。

逻辑错误导致的问题，只需改正，无需防御代码

---------------------------------------------------

5. 对象管理审计 Object Management Audit
核心： 对象类型可能不对

说明：

在 SUI 中，对象可以被转换为共享对象（Shared Object），这意味着对象的访问权限可能从私有变为公共。因此，需要对所有使用的对象进行详细审查，明确每个对象是静态的还是共享的。特别要注意是否有对象被错误地从私有对象转换为共享对象，这样可能导致未经授权的用户访问这些对象，带来潜在的安全风险。

定位：

整理并分析模块中所有涉及的对象，检查对象的类型和权限设置，确保该对象的权限与业务需求匹配。如果发现私有对象被错误地转换为共享对象，必须标记为潜在风险并提出修正建议。

-----------------------------------------------------

6. Token 代币消耗审计 Token Consumption Audit
核心： coin是object，注意它金额，存在与所有权等是否按照预期进行

说明：

SUI 的代币模型与其他链的代币模型有所不同。SUI 允许对象持有代币，并且代币对象可以嵌套在其他对象中，还可以进行拆分。因此，在涉及代币消耗的场景中，需要特别关注代币的管理和流转，以避免安全问题或意外损失。

定位：

在审计代币消耗时，需要检查以下关键点：

(1) 消耗的金额是否准确。
(2) 代币对象是否已经正确转移。
(3) 代币的拆分和合并是否合理。
(4) 检查代币与对象的绑定。

具体： https://chat.deepseek.com/share/zp05bm1otvk6pjvkbn

-------------------------------------------------------

7. 闪电贷攻击审计 Flashloan Attack Audit
核心： 拿到大量资金用来操纵价格(价格预言机操纵或者冲池子等)，或者干脆就是闪电贷代码直接写错了，比如可见性与访问控制，所以本质上就要么是你闪电贷过程中这笔资金出问题了，要么是大额资金可能引起的问题
核心： 审计要点：
(1) 价格预言机操纵
✅ 是否使用时间加权平均价格（TWAP）而非瞬时价格？

✅ 是否依赖单一 AMM 池作为唯一价格来源？单一来源极易被闪电贷临时操纵。

✅ 是否结合了多个预言机来源（如 Pyth、Switchboard 等 Sui 生态原生预言机）？

✅ 是否实现了价格偏离阈值检测与熔断机制（超过阈值则拒绝执行）？

(2) 协议算法的安全性： https://chat.deepseek.com/share/2nse699ycb6rtvhfuz
检查位运算溢出，且如果使用自定义数学库需逐行检查
✅ 流动性计算公式、费率计算等核心业务逻辑是否存在数值边界漏洞？

✅ 是否有极端参数输入（如极大 tick、极小流动性区间）可以触发意外行为？

(3) 函数可见性与访问控制
✅ 内部函数是否误用 public 可见性？

✅ 与资金转移相关的关键函数是否有适当的访问控制（如 sui::tx_context::sender() 校验）？

✅ 是否存在"查询函数"意外修改状态的情况？Move 本身不区分 view 和写操作，需人工识别。

✅ entry 函数的参数是否能被攻击者构造出恶意输入？


说明：

Sui 的 Move 也有闪电贷的用法(Hot Potato)。用户可以在一笔交易中借取大量的资金任意使用，只需在这笔交易内归还资金即可。恶意用户通常使用闪电贷放大自身的资金规模进行价格操控等大资金攻击。

定位：

分析协议本身的算法(奖励、利率等)、预言机依赖是否合理。

详细阐释（包括稳定币的详细阐释）： https://chat.deepseek.com/share/nhkicg5u5m3u00f1le

-------------------------------------------------------

8. 权限漏洞审计 Permission Vulnerability Audit
核心： 找清楚接口(public, entry之类)，标注敏感操作(transfer, delete, update, withdraw之类），看清楚所有权，梳理流动

说明：

在 Sui 的 Move 合约中权限漏洞这部分和业务的需求以及功能设计关系较大，所以遇见较为复杂的 Module 的时候，就需要和项目方一一确认各个方法的调用权限。这里的权限一般是指函数的可见性和函数的调用权限。

定位：

· 检查和确认所有函数方法的可见性及调用权限，在项目评估阶段就需要项目方提供设计文档，审计时候根据设计文档中的描述一一确认权限。

· 梳理项目方角色的权限范围，如果项目方角色的权限会影响用户的资产，则存在权限过大的风险。

· 分析对外函数里面传递进去的对象是什么类型的对象，如果是一些特权函数那必须要有一些特权对象参与。

详细阐释： https://chat.deepseek.com/share/h2otsd6j6k6kd0isao

-------------------------------------------------------------------

9. 合约升级的安全审计 Smart Contract Upgrade Security Audit
核心：是否成功把旧版本换成新版本
核心总结：
审计项目	          关键问句	                                             常见高危信号
初始化逻辑	      init 中的代码是否在升级后通过迁移函数重新执行？	         升级后运行报 Object not found。
依赖更新	          引用的外部包地址是否已过时且存在已知漏洞？	             Move.toml 中依赖项未更新 rev。
动态字段迁移	      是否检查了所有 dynamic_field Key 的命名空间变更？	     旧 Key 直接丢弃，数据丢失。
对象 ID 稳定性	  迁移后关键对象的 ID 是否改变？	                         硬编码 ID 的下游服务停止工作。
防重放保护	      迁移函数是否包含 assert! 确保仅执行一次？	             可被反复调用刷取奖励或导致状态混乱。


说明：

在 Move 中，外部模块通过 use 关键字导入。需要注意的是，Sui 的合约是可升级的，但发布的合约包（package）是不可变的对象，一旦发布就无法撤回或修改。合约升级的本质是通过在新的地址上重新发布更新的合约，并将旧版本合约的数据迁移至新的合约中。因此，合约升级过程中需要特别注意以下几点：

· init 函数：init 函数仅在合约第一次发布时执行，后续合约升级时不会再次触发。
· 升级合约不会自动更新依赖：如果您的合约包依赖于外部包，当外部包升级时，您的合约包不会自动指向升级后的合约地址。您需要手动升级自己的合约包以指定新的依赖项。
定位：

需要对合约升级过程中数据迁移的逻辑进行详细检查，确保迁移操作安全、准确，并避免遗漏重要数据或依赖更新的问题。

详细阐释： https://chat.deepseek.com/share/0qnez15krx3y77xpih
          https://chat.deepseek.com/share/6oudbojvf2jhclrldy

--------------------------------------------------------------------------

10. 外部调用安全审计 External Call Function Security Audit
核心： 外部模块稳定性与可信度， 访问权限有误， 复杂调用产生的问题(过程中产生的时间差，错误调用，以及递归调用导致的资源耗尽)

说明：

与外部模块使用审计项相同，由于 Move 中进行外部调用需要先将外部模块导入，因此理论上外部调用的结果都是开发人员所预期，所需主要的是外部模块的稳定性。

定位：

需要对外部引入的库进行检查。

详细阐释： https://chat.deepseek.com/share/vr6aqpf09xt9slkjud

------------------------------------------------------------------------------

11. 返回值的检查 Unchecked Return Values
核心： 标准库函数或自定义函数设计为 返回布尔值或错误码 来表示成功/失败，而不是直接中断。如果调用方不检查这个返回值，交易仍会继续执行。所以尤其关注所有的函数返回类型，警惕bool，Option<T>和Result<T, E>

说明：

在 Move 合约中，和其他智能合约语言类似，某些函数的返回值需要进行检查。如果忽略了对这些返回值的处理，可能会导致关键逻辑没有正确执行，进而引发安全问题。

定位：

需要检查代码中每个函数调用的返回值，特别是那些涉及外部调用或重要状态更新的函数。如果返回值未被处理或验证，可能会导致不可预期的行为，应该标注为潜在风险点。

详细阐释： https://chat.deepseek.com/share/ivcf3azinnjsvw69oa

--------------------------------------------------------------------------------

12. 拒绝服务审计 Denial of Service Audit
核心： 由循环与遍历规模过大导致的gas费过大 (或者产生的其它问题，比如上限250kb带来的安全问题，但凡是计算机处理必有上限，所以无限操作一定是有问题)，或传入有误数据(比如传入错误泛型或者错误共享对象ID，或者有问题的外部依赖等等)，或者大速率冲共享对象，总之就是通过冲边界或者错误来破坏交易

说明：

拒绝服务（DoS）攻击可能由代码逻辑错误、兼容性问题或其他安全漏洞引发，导致智能合约无法正常运行。此类问题可能影响合约的可用性，甚至使其完全瘫痪。

定位

· 重点检查业务逻辑的健壮性，确保在各种情况下都能正常执行，不会因错误或漏洞导致合约中断。

· 关注与外部模块交互的部分，确保其兼容性，以防止由于外部依赖问题导致的服务中断。

详细阐释： 
https://chat.deepseek.com/share/2n013lv1fnpqhagren
https://chat.deepseek.com/share/68h7cf8p5rjl1j6u3u
https://chat.deepseek.com/share/xkpl8f1nwspbes8zhj

----------------------------------------------------------------------------------

13. Gas 优化审计 Gas Optimization Audit
核心： 不影响功能情况下越简单越好(比如把链上操作搬到链下做，再传结果回来，这样可以简化链上流程，但这过程中产生的交互和时间差本身也可能成为被攻击的对象，所以需充分检查)

说明：

与以太坊一样，Sui 也有 Gas 机制，任何模块脚本调用都会消耗 Gas。因此对一些冗长且高复杂度的代码进行优化是有必要的。

定位：

· 涉及到复杂的调用看是否可以解耦。
· 涉及到高频率的调用看是否可以优化函数内部执行的效率。

详细阐释： https://chat.deepseek.com/share/sc12jlish8v64af4x4

--------------------------------------------------------------------------------------

14. 设计逻辑审计 Design Logic Audit
核心： 梳理逻辑，控制权限与所有权，对比实际情况与业务设计

说明：

设计逻辑审计的重点是检查代码中的业务流程和实现，确认是否存在设计缺陷或与预期不符的情况。代码实现如果与预期逻辑不一致，可能会导致意外的行为或安全风险。

定位：

· 根据不同角色的权限和作用范围，梳理业务流程中的可能调用路径。
· 确定每个业务流程所涉及的数据范围，确保数据的操作与业务设计一致。
· 将实际的调用路径与预期的业务流程进行比较，识别并分析任何可能导致非预期结果的调用情况。

详细阐释： https://chat.deepseek.com/share/1x2wbe977adztk2ape


----------------------------------------

drop并非必需品，drop只是隐式自动销毁，相当于只是一个销毁功能的快捷键，其实际功能完全可以通过直接显式用销毁函数实现，加上drop功能本身就有很大风险被误销毁掉(具体原理看drop功能阐释)，所以对于审计来说，但凡是有drop的地方都值得高度警惕 (可检查可见性)
copy能力虽然在某些场景下属于必需，但仍需高度警惕，审查其逻辑 (可检查可见性) 
(这两点入权限)

-------------------------------------------------------

集合安全：避免灾难性存储溢出 (入DoS)
· 已知大小≤1000： 使用VecSet/VecMap
· 位置大小或第三方添加：使用Table/Bag
· 资产存储： 强制使用ObjectBag/ObjectTable
· 禁止在循环中使用vector:push_back处理用户输入

--------------------------------------------------------

                 检查要点                                                                方法/工具
事件日志        | 关键操作是否记录事件                                                 |   日志审计
升级机制        | 是否有紧急暂停功能(并且这种暂停功能本身也需要验证是否会有DOS风险？)      |   功能测试

---------------------------------------------------------

测试验证

1. 单元测试： 覆盖边界条件
2. 覆盖率测试： 消灭盲点
3. 模糊测试： 发现隐藏漏洞

----------------------------------------------------------

安全发布流程
Step1: 测试网部署
Step2: 监控72小时
Step3: 异常检测
if result == no
  Step4: 审计通过
  Step5: 主网分阶段部署
  Step6: 1%流量测试
  Step7: 50%流量测试
  Step8: 100%部署
else
Step4: 回滚修复

-------------------------------------------------------------

链上监控与响应 
(1) 关键指标监控
· 异常交易频率
· 权限变更事件
· 余额异常波动
· 函数调用失败率

(2) 应急响应流程
Step1： 触发紧急暂停
Step2： 分析攻击路径
Step3： 部署修复版本
Step4： 执行资产恢复
Step5： 发布事后报告





